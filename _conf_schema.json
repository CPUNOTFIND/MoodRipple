{
  "initial_mood": {"type": "int", "default": 0, "description": "初始心情", "hint": "机器人首次运行时的全局心情值，范围 -100 到 100。之后会持续保存，不会每天重置。", "obvious_hint": true},
  "daily_decay_rate": {"type": "float", "default": 0.12, "description": "每日回归", "hint": "每天向中性心情回归的比例，范围 0 到 1。数值越大，情绪惯性越弱。", "obvious_hint": true},
  "significant_change_threshold": {"type": "int", "default": 12, "description": "标签阈值", "hint": "心情单次变化达到此幅度时，才会请求 AI 刷新情绪词条。", "obvious_hint": true},
  "max_emotion_labels": {"type": "int", "default": 4, "description": "词条上限", "hint": "AI 为当前心情保留的最大情绪词条数。", "obvious_hint": true},
  "event_time_windows": {"type": "list", "default": ["09:00-11:30", "14:00-17:30", "20:00-22:30"], "description": "事件时段", "hint": "每天随机事件可出现的本地时间窗口，格式为 HH:MM-HH:MM。", "obvious_hint": true},
  "max_daily_events": {"type": "int", "default": 3, "description": "每日事件数", "hint": "一天内最多生成多少个 AI 随机情绪事件。默认 3 可覆盖默认的早、中、晚三个窗口；若插件在当天中途启动，只会安排尚未结束的窗口。", "obvious_hint": true},
  "scheduler_timezone_offset": {"type": "float", "default": 8, "description": "调度时区", "hint": "事件窗口和日记日期使用的固定 UTC 时区偏移。中国标准时间填写 8；云服务器即使运行在 UTC，也会按此处时间触发。范围 -12 到 14。", "obvious_hint": true},
  "event_flirtiness": {"type": "int", "default": 1, "description": "暧昧强度", "hint": "事件的聊天张力：0 为普通，1 为俏皮，2 为轻度暧昧，3 为更强的含蓄成人氛围。所有级别均禁止露骨性内容及任何年龄或同意假设。", "obvious_hint": true},
  "chat_atmosphere_ratio": {"type": "float", "default": 0.35, "description": "氛围参考率", "hint": "随机事件参考已提炼的匿名群聊氛围摘要的概率，范围 0 到 1。", "obvious_hint": true},
  "proactive_user_ids": {"type": "list", "default": [], "description": "主动名单", "hint": "允许机器人主动联系的 QQ 号。即使尚无对话记录，也会使用下方 UMO 模板直接发起私聊。", "obvious_hint": true},
  "proactive_umo_template": {"type": "string", "default": "default:FriendMessage:{qq}", "description": "主动路由模板", "hint": "没有历史会话时构造私聊目标的 UMO。OneBot 默认格式为 default:FriendMessage:{qq}；若平台实例名不同，请替换 default，{qq} 会自动替换为 QQ 号。", "obvious_hint": true},
  "proactive_probability": {"type": "float", "default": 1.0, "description": "主动概率", "hint": "每个随机事件后尝试主动联系一名符合条件的名单用户的概率，范围 0 到 1。默认 1 表示每次事件都尝试；最近 20 分钟活跃或仍在冷却的用户会先排除，再从剩余用户中选择。", "obvious_hint": true},
  "proactive_cooldown_minutes": {"type": "int", "default": 720, "description": "消息冷却", "hint": "对同一用户两次主动消息之间的最短间隔，单位为分钟。", "obvious_hint": true},
  "proactive_batch_interval_seconds": {"type": "float", "default": 1.5, "description": "批量发送间隔", "hint": "仅用于 /mooddebug flow：向主动名单逐个提交消息时的等待秒数。较低带宽或 QQ 限流环境建议保持 1.5 秒或更高，范围 0.2 到 10。", "obvious_hint": true},
  "proactive_send_timeout_seconds": {"type": "float", "default": 15, "description": "发送超时", "hint": "向单个 QQ 适配器提交主动消息的最长等待秒数，范围 3 到 60。网络异常时会及时释放调度循环，避免整批流程长期卡住。", "obvious_hint": true},
  "affection_sensitivity": {"type": "float", "default": 1.0, "description": "好感敏感度", "hint": "AI 给出的好感度变化的总敏感度。", "obvious_hint": true},
  "affection_boundary_damping": {"type": "float", "default": 0.75, "description": "边界阻尼", "hint": "好感度接近 -100 或 100 时的减速强度，越大越难抵达极端值。", "obvious_hint": true},
  "group_atmosphere_enabled": {"type": "bool", "default": true, "description": "群聊氛围", "hint": "开启后，仅在机器人近期参与互动的群中收集短暂内存文本并提炼匿名群体氛围。", "obvious_hint": true},
  "group_whitelist": {"type": "list", "default": [], "description": "群聊白名单", "hint": "允许感知氛围的群号列表；留空代表不限制群号。", "obvious_hint": true},
  "group_active_minutes": {"type": "int", "default": 20, "description": "群活跃窗口", "hint": "机器人在群里回复后，多长时间内允许进行群聊氛围感知，单位为分钟。", "obvious_hint": true},
  "group_batch_size": {"type": "int", "default": 8, "description": "氛围批量", "hint": "累计多少条匿名群消息后才调用一次 AI 分析群体氛围。", "obvious_hint": true},
  "journal_hour": {"type": "int", "default": 0, "description": "日记时间", "hint": "每天从该小时起，自动补写前一天的内部情绪日记，范围 0 到 23。", "obvious_hint": true},
  "ai_provider_id": {"type": "string", "default": "", "description": "内部模型", "hint": "专用于情绪评估、词条、事件、日记和主动消息的模型提供商；留空使用 AstrBot 当前默认模型。", "obvious_hint": true, "_special": "select_provider"},
  "internal_ai_timeout_seconds": {"type": "float", "default": 90, "description": "模型超时", "hint": "内部 AI 单次请求的最长等待秒数，范围 15 到 300。超时后本次任务会安全失败；定时事件会在下一轮调度重试。", "obvious_hint": true},
  "enable_qq_status_sync": {"type": "bool", "default": false, "description": "状态同步", "hint": "开启后，若 QQ 适配器显式提供状态接口，插件会在随机事件后最佳努力同步诗意签名或状态。", "obvious_hint": true}
}
